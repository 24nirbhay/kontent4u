import { create } from 'zustand'; const useAppStore = create((set) => ({ activeTab: 'The Arena', setActiveTab: (tab) => set({ activeTab: tab }), targetAudienceProfile: '', setTargetAudienceProfile: (profile) => set({ targetAudienceProfile: profile }), scrapedTrendsCache: [], setScrapedTrendsCache: (trends) => set({ scrapedTrendsCache: trends }), arenaAgentTurnState: 0, setArenaAgentTurnState: (turn) => set({ arenaAgentTurnState: turn }), finalGeneratedScript: '', setFinalGeneratedScript: (script) => set({ finalGeneratedScript: script }), resetWorkspace: () => set({ targetAudienceProfile: '', scrapedTrendsCache: [], arenaAgentTurnState: 0, finalGeneratedScript: '' }) })); export default useAppStore;
